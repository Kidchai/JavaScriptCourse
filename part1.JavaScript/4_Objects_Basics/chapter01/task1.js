/*
Write code to perform the task in each item as a separate line:

1. Create an empty object 'user'.
2. Add a property 'firstName' with a value of 'John'.
3. Add a property 'lastName' with a value of 'Smith'.
4. Change the value of the 'firstName' property to 'Pete'.
5. Remove the 'firstName' property from the object.
*/

let user = {};
user.firstName = "John";
user.lastName = "Snith";
user.firstName = "Pete";
delete user.firstName;