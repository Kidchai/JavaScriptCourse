/*
Write an if statement to check if the value of the variable 'age' is NOT 
within the range of 14 and 90, inclusive.

Write two versions: one using the NOT operator !, and the other without it.
*/
if (!(age >= 14 && age <= 90));
if (age < 14 || age > 90);